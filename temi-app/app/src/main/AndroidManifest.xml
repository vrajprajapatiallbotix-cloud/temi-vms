<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Temi required permissions -->
    <uses-permission android:name="com.robotemi.permission.MAP" />
    <uses-permission android:name="com.robotemi.permission.FACE_RECOGNITION" />
    <uses-permission android:name="com.robotemi.permission.SETTINGS" />
    <uses-permission android:name="com.robotemi.permission.SEQUENCE" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:name=".TemiVMSApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.robotemi.sdk.action.home" />
            </intent-filter>
            <!-- Temi kiosk home screen -->
            <meta-data
                android:name="@string/metadata_kiosk"
                android:value="true" />
        </activity>

        <activity android:name=".ui.QRScanActivity" android:screenOrientation="landscape" />
        <activity android:name=".ui.VisitorDisplayActivity" android:screenOrientation="landscape" />
        <activity android:name=".ui.ErrorActivity" android:screenOrientation="landscape" />

    </application>
</manifest>
